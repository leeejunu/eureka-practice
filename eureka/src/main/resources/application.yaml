spring:
  application:
    name: eureka
server:
  port: 8761

eureka:
  client:
    register-with-eureka: false #eureka 자기 자신은 서버에 등록할 것인가(자기 자신은 등록할 필요없음) default(true)
    fetch-registry: false #등록되어진 서비스의 목록을 주기적으로 가져올 것인가

